public class ReferenceTrap {
    public static void main(String[] args) {
/**
 * A variable cannot store an array
 * It stores a reference that points to it due to that another variable can store a reference that points
 * at the same array.
 */

/**
 * To prevent this creates  a new array, copy everything from the old array into the new array using a
 * for loop then update the array at what ever index you want.
 */

/**
 * Arrays.copyOf(array, array.lenght);
 * it takes two arguments, the array you will like to copy and how much of it you will like to copy.
 *
 */


    }
}
